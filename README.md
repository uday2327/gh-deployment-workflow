# gh-deployment-workflow

Demo: deploy index.html to GitHub Pages using GitHub Actions.

How it works:
- Workflow triggers on push to main when index.html changed.
- Workflow packages files into `public/` and uses GitHub Pages actions to publish.

URL: https://<uday2327>.github.io/gh-deployment-workflow/
